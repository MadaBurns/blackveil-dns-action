name: Repo Hygiene

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  hygiene:
    uses: MadaBurns/bv-mcp/.github/workflows/repo-hygiene.yml@main
