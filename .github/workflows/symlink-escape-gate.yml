name: Symlink escape gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  symlink-escape:
    uses: MadaBurns/bv-mcp/.github/workflows/symlink-escape-gate.yml@main
